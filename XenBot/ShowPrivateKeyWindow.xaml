<Window x:Class="XenBot.ShowPrivateKeyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:XenBot"
        mc:Ignorable="d"
        Title="Show Private Key" Height="110" Width="800">
    <Border Margin="5">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Text="Private Key" FontSize="19" Grid.Column="0" Grid.Row="0" Margin="0 0 5 5" VerticalAlignment="Center" />
            <TextBox x:Name="tbPrivateKey" Grid.Column="1" Grid.Row="0" VerticalAlignment="Center" />
            <Button Content="Close" Grid.Column="1" Grid.Row="1" Width="100" HorizontalAlignment="Right" Click="Button_Click"  />
        </Grid>
    </Border>
    
</Window>
